# Build artifacts
/target
**/*.rs.bk

# Generated libraries
/lib/*.a
/lib/*.so
/lib/*.dylib

# Downloaded tarballs (if any remain)
*.tar.gz

# Test artifacts
test.pdf
test_output.txt

# Editor/System
.DS_Store
.vscode
.idea
*.swp

# We keep vendor/ and tessdata/ for self-containment as requested by the nature of the project (static build), 
# unless we move to a script-based setup. For now, we commit matches standard "vendor everything" for C deps in Rust sometimes.
